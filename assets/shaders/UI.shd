renderpass=UI.rnp
stages=vertex,fragment
stagefiles=shaders/UI.vert.spv,shaders/UI.frag.spv
useInstance=1
useLocal=1
renderOrder=100

attribute=vec3,inPosition
attribute=vec3,inNormal
attribute=vec2,inTexcoord
attribute=vec4,inColor
attribute=vec4,inTangent

uniform=samp,1,0,diffuseTexture

push=mat4,model