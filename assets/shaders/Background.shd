renderpass=Background.rnp
stages=vertex,fragment
stagefiles=shaders/Background.vert.spv,shaders/Background.frag.spv
useInstance=0
useLocal=0
renderOrder=-100

attribute=vec3,inPosition
attribute=vec3,inNormal
attribute=vec2,inTexcoord
attribute=vec4,inColor
attribute=vec4,inTangent

uniform=samp,0,0,diffuseTexture